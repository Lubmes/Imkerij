<div id='shop-edit' class='category'>
	<div id='pictures'>
		<%= render 'pictures/edit_pictures', imageable_model: @category %>
	</div>

	<div id='details'>
		<%= render 'form', category: @category %>
	</div>
</div>
