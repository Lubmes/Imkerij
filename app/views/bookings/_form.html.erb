<%= form_for booking, remote: true do |f| %>
	<%= f.hidden_field :product_id, value: product.id %>
	<tr>
    <th><%= f.label :product_quantity %></th>
    <td><%= f.number_field :product_quantity, value: 1, min: 1 %></td>
  </tr>
	<tr>
  	<td colspan='2'>
			<%= f.submit 'VOEG TOE AAN MANDJE' %>
  	</td>
	</tr>
<% end %>
