<div id='controls'>
	<h2>AFREKENEN</h2>
	<%= link_to 'Bevestig', [:to_bank, @order] %>
</div>

<header>
	<h3 class='co-order-col'>Controleer uw bestelling!</h3>
	<h3 class='co-user-col'>
		<% if user_signed_in? %>
			Controleer uw verzendgegevens!
		<% else %>
			Uw gegevens zijn nodig voor verzending
		<% end %>
	</h3>
</header>

<div id='payment'>
	<div id='order' class='co-order-col'>
		<%= render @order %>
	</div>

	<div id="user-info">
		<div class="co-user-col">
			<% if user_signed_in? %>
				<%= render @user %>
				<% unless @delivery.nil? %>
					<%= render @delivery %>
				<% else %>
					<header>
						<h3 class='add-address'><i class='fa fa-plus'></i> ADRES</h3>
					</header>
					<%= render 'deliveries/form', delivery: @user.deliveries.build %>
				<% end %>
			<% else %>
				<div class='user'>
					<%= render 'devise/registrations/form', with_header: true %>
					<%= render 'devise/sessions/form', with_header: true %>
				</div>
			<% end %>
		</div>
	</div>

</div>
