<% if deliveries.any? %>

    <% deliveries.each do |delivery| %>
      <div id='delivery-ajax-<%= delivery.id %>'>
        <%= render delivery,
              card_title: "#{delivery.address_zip_code}, #{delivery.address_street_number}",
              order: order %>
      </div>
    <% end %>

<% end %>
