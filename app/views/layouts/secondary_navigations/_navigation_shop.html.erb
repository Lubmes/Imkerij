<% content_for :navigation_top_margin do %>
	<!-- niets -->
<% end %>

<nav id='sec-nav' class='shop'>
	<% @categories.each do |category| %>
		<%= link_to category.name.downcase, "#cat-#{category.id}" %>
	<% end %>
</nav>
