<div id='shop-edit'>
	<div id='details' class='details-col'>
		<%= render 'form', category: @category, product: @product %>
	</div>

	<div id='pictures' class='pictures-col'>
		<%= render 'pictures/pictures', imageable_model: @product %>
	</div>
</div>
