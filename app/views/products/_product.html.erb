<div class='description'>
  <% if current_page?(action: 'index') %>
    <%= truncate(product.description, length: 200) { link_to "meer", [product.category, product] } %>
  <% else %>
    <%= product.description %>
  <% end %>
</div>
